{
  "builds": [
    {
      "src": "**/*.rb",
      "use": "@vercel/ruby",
      "config": {
        "command": "rails server -p $PORT"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/public/$1"
    }
  ]
}
